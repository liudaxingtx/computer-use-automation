# 计算机使用自动化系统 — 设计与策略（中文版 · 仅本地）

> 本文件是英文版 `DESIGN.md` 的中文对照，只留在本地（已被 gitignore），方便达哥自己阅读追踪。提交给面试官的是英文版。

---

## 1. 问题重述

interface.ai 为银行和信用合作社做 AI agent。这些 agent 要操作的大部分后台软件——核心银行界面、服务工具、管理控制台——**没有 API**。唯一的入口就是像人一样去驱动 UI。

这个项目就是「给 agent 装上手的后端集成层」：一个系统，能做到

1. 用 LLM **发现**怎么在真实界面上完成一个任务，
2. 把这次成功的运行**录制**成结构化、可复用的 artifact，
3. 再**确定性回放**它——决策循环里没有 LLM——让 agent 在生产里可靠、廉价地调用它。

真实环境的三个特性，决定了我们每一个决策：

1. **界面稳定，但运行时错误是真的。** 企业 UI 变化慢（这正是"录一次/放多次"可行的原因），但回放必须处理运行时条件：校验错误、"查无此人"、权限拒绝、意外弹窗、会话超时、慢加载、直接报错。只走 happy path 的能力在生产里没用。
2. **异构、常是 legacy 界面。** 现代 web、legacy web（服务端渲染、frameset、深嵌套表格、无语义标记、无 test ID）、甚至原生桌面。不能假设有干净的 DOM。
3. **多租户大规模。** 几百家机构、上千个应用实例，很多跑的是同一个厂商产品，只是配置/品牌/版本不同。自动化应当能泛化——或优雅降级——而不是每家重写一遍。

## 2. 我们的核心论点

> **模型负责发现。artifact 变成可复用能力。确定性回放是 agent 在生产里调用它的方式。**

一句话，整个构建过程都要时刻绷着：

> **LLM 只出现一次——在 discovery 阶段。之后它就退出循环。**

下面每一个决策，都是为了守住这个分离。它是这道题的全部意义，也是「真正的设计」和「一个套了 Playwright 的 AI 包装」的分水岭。

## 3. 我们在优化什么

评估权重（大致顺序）：**系统设计**（artifact schema + 回放契约是核心）→ **核心循环正确性** → **健壮性与错误处理** → **人类介入升级** → **泛化** → **安全**。功能广度和过早的基础设施，明确**不**加分。

所以我们的深度砸在三个**承重墙**上，其余都保持"薄但真实"：

1. **Artifact schema** —— schema 和回放契约。
2. **确定性回放 + 错误分类** —— 把"预期业务结果"和"可恢复条件"和"硬失败"分开。
3. **人类介入交接** —— 真实的控制转移机制，不是 TODO。

## 4. 关键决策及理由

### 4.1 为什么是这三个承重墙

- **Artifact schema** 是"一次性智能"变成"可复用能力"的地方。schema 错了，回放就没地基。它必须**带类型、带版本、可评审**，而且关键是要**和原始 LLM 对话记录解耦**——我们存的是提炼后的步骤，不是模型当时的碎碎念。
- **回放 + 错误分类** 是生产路径。题目明点名的"最常犯的设计错误"，就是把业务结果（"查无此人"）和失败混为一谈。我们把结果契约建成三态，再单独分一层"恢复 vs 硬失败"。
- **交接** 是"卡住时安全停下"变成"银行真的敢跑"的地方。它必须是同一个 live session 上真实的 暂停 → 让出 → 恢复 状态机。

### 4.2 观察：无障碍树为主，视觉兜底，截图做证据

我们用**三只眼睛观察界面，各管一件事**：

1. **无障碍树（主）**。浏览器/OS 为屏幕阅读器暴露的语义表示（role、name、值）。它是**文本**（便宜、纯文本模型就能用），而且比原始 DOM **更稳定**，因为它捕捉的是人"读到"的语义，不是实现细节。对没有干净 DOM、没有 test ID 的 legacy 应用，这是对的默认。
2. **视觉模型（兜底——Kimi K3）**。当无障碍树缺失、或界面真的是图形（canvas、无语义 UI），我们截图让视觉模型**理解**页面。这比 OCR 强，因为视觉模型读的是**语义**（"这是个确认框""这是条红色报错"），不只是抠字。
3. **截图（证据）**。失败时持久化一张截图/DOM 快照，作为题目要求的"更丰富信号"——给调试和人看。

这三者是叠加，不是竞争。

### 4.3 关键的分离：理解 vs 定位 vs 执行

这是全项目承重墙里的承重墙：

| 阶段 | 问题 | 谁来回答 | 什么时候用 |
|------|------|----------|-----------|
| **理解** | "我在哪个页面？下一步该干啥？" | LLM（DeepSeek 做结构化决策，K3 做视觉理解） | 只在 **discovery** |
| **定位** | "我要的控件在哪？怎么稳健地指到它？" | **artifact**（定位策略，不是截图） | 录一次，永远用 |
| **执行** | "找到它、操作它、验证它。" | **回放引擎** | **replay**，无 LLM |

我们拒绝踩的坑：如果回放还要重新跑视觉模型来找元素，那 LLM 又回到了循环里，"确定性回放"就是假的。所以：

> **K3 只在 discovery 看一次界面。artifact 记录的是"怎么定位"每个控件。回放靠 locator 定位，绝不再看一眼。**

### 4.4 目标应用：一个故意"难啃"的本地 mock

我们**不**对公开网站自动化（ToS、限流、错误状态不可控）。而是自己建一个小的、本地 mock 的 legacy 银行后台应用，故意做成**难啃**：表格布局、无 test ID、深嵌套标记，并且埋几个能按需触发的运行时状态：

- 一个"查无此人"的查询（合法**业务结果**），
- 一个确认弹窗（**可恢复条件**），
- 一个权限拒绝状态（**硬失败**）。

这样我们能**证明**错误分类不是嘴上说说，也能确定性地产出题目明确要的"命中错误的回放"证据。

### 4.5 架构：单进程，清晰模块

题目明确不建议建队列/集群/多租户管道。我们保持单进程，六个模块边界清晰：

```
agent loop  →  artifact model  →  replay engine
                 ↑                    ↑
              safety/guardrails    escalation/handoff
                 └─────── evidence/observability ───────┘
```

### 4.6 技术栈

- **语言/运行时：** Python 3.12
- **浏览器自动化：** Playwright（accessibility API + 截图）
- **决策 LLM：** DeepSeek（结构化 JSON 输出，做 observe→decide→act 循环）
- **视觉 LLM：** Kimi K3（截图理解兜底）
- **Artifact 建模：** Pydantic（天然带类型 + 带版本）

## 5. Artifact schema（草图）

artifact 是一个 **Capability**：一个可复用流程的、带类型、带版本的描述。

```
Capability
├─ meta: name, version, description, surface (browser|desktop), created_at
├─ inputs:  [{name, type, required, description}]        # 如 member_id: str
├─ outputs: [{name, type, source}]                        # 如 member_name, account_status
├─ checkpoint: 怎么知道目标达成了                          # 如 "摘要面板出现 + status == Active"
└─ steps: [
    {
      action: click | type | select | navigate | read | wait
      value: "<加密的客户输入，如 member_id>"   # AES-256-GCM；仅回放时解密
      target: {
        strategy: accessibility | text | css | xpath      # + fallback 链
        value: "role=button name='Search'"
        reasoning: "为什么选这个定位"                       # 给人评审 + 将来修复用
      }
      assert: { expect: "...", on_mismatch: ... }         # 每步 checkpoint，不只是最后
      on_error: { outcome: business|recoverable|hard, then: ... }
    }
  ]
```

设计原则（这些是"为什么"）：

1. **意图，不是坐标。** 我们记的是"对哪个控件做什么、怎么指到它"，绝不记绝对像素/时序。回放每次都重新解析定位——既确定，又扛得住良性布局位移。
2. **和对话记录解耦。** artifact 是提炼后的、带类型的步骤——人评审和调用 agent 都能读懂，而且哪怕 discovery 时模型反悔了六次，它也不受影响。
3. **每一步都带 assert。** checkpoint 是每步一个，不只是最后——绝不假设"我点了就等于成了"。
4. **错误是一等公民。** 每一步声明怎么分类、怎么处理它的失败模式，喂给回放引擎的决策树。
5. **人可管理 —— 评审、编辑、版本。** artifact 是纯可 diff 的 JSON，人能看到每个定位「为什么这么选」（reasoning），当目标网站变了就改它（改按钮名、改路由）、bump 版本号、再跑一次 dry-run 回放验证——不用重跑昂贵的 discovery。小漂移 = 手改定位；大改动 = 触发一次新 discovery 并评审新版本。artifact 被当成代码对待：有版本、可评审、可修复。
6. **客户数据静态加密。** 用户实际输入的值——会员 ID、姓名、金额、账号——属于客户，不属于我们。artifact 记录某一步的输入值时，写入前先用 per-tenant 密钥做 AES-256-GCM 加密；回放只在用到的那一刻才解密。定位策略（role/name/reasoning）保持明文——那是 UI 结构，不是客户数据——所以 artifact 依然可评审、可修复，而敏感载荷即使对内部操作员也保持不透明。

## 6. 确定性回放 + 错误分类

回放**不是**盲执行；是每一步 操作 → 断言 → 分支。

结果契约恰好三种形态：

1. **success** —— 目标达成，返回带类型的 `outputs`。
2. **business_outcome** —— 一个合法的、调用方需要的预期答案（"查无此人"、"已处理过"）。不是崩溃。
3. **failure** —— 硬停，带可调试诊断：哪一步、期望什么、实际看到什么。

恢复和失败分开：

- **可恢复条件**（关掉已知插页、等待/重试瞬时加载、超时后重登）——带边界重试内联处理。
- **硬失败**（权限拒绝、结构意外、重试耗尽）——停下、报清晰错误，必要时转人类升级（§8）。

## 7. 可观测性 + 修复闭环

确定性回放足够便宜，可以不停跑——所以每一次运行都可以被**记录**、被**度量**、被**闭环修复**。这就是把「确定性执行器」变成「银行敢跑很多年」的东西。

**每一次回放都产生一条 ReplayRun 记录：** `capability`（name + version）、`inputs`、`started_at`、`duration`，以及 `result`：`success` / `business_outcome` / `failure`。失败时，记录还带上失败的那一步、`expected` vs `observed`、错误分类（recoverable / hard / business），以及一张截图 + DOM 快照给人评审。受监管金融数据绝不写进这里（见「安全与护栏」）。

**成功率统计。** 按 capability 聚合——也按版本、按租户——统计成功率、业务结果率、失败率，以及最近 N 次的趋势。某个 capability 成功率突然下滑，就是金丝雀：目标网站漂移了，artifact 需要修复——**在它变成一张工单之前**。

**失败案例库。** 每一次非 success 的运行都落进一个可检索的案例库，带完整上下文。每个案例都可以**回放**：「回放这个错误」用完全相同的 capability + inputs 重跑，确定性地复现失败——维护人员看到的是人当时会看到的东西，不是一段文字摘要。

**修复闭环 —— 监控 → 诊断 → 修复 → 复查：**

1. **监控** —— 统计标记出某个 capability 在退化。
2. **诊断** —— 打开失败案例、回放错误、读失败那一步的 expected-vs-observed 和截图。
3. **修复** —— 两条路：**原样重试**（怀疑是瞬时问题）重跑同一版本；**修复 + bump**（网站变了）手改定位（设计原则 #5）并 bump 版本号。
4. **复查** —— 用**原始失败输入**对修复后的版本重放；通过就把案例标记为已解决。闭环靠证据收口，不靠祈祷。

**人工监察。** 一个 CLI 暴露统计总览、失败案例库、以及每个案例的详情（回放 / 标记已解决 / bump）。人能审计整条链路——什么坏了 → 我改了什么 → 证明修好了——这正是银行真正需要的运营保障。

## 8. 人类介入交接

同一个 **live session** 上真实的控制转移状态机：

```
AUTOMATION ──卡住/危险/不可逆──▶ PAUSED ──操作员接管──▶ HUMAN
    ▲                                                       │
    └────────────── 操作员信号恢复后继续 ────────────────────┘
```

- **检测与路由：** 识别阻塞状态，向人类操作员发起干预请求，带上能力/目标、当前步骤、截图/DOM 快照、以及"为什么停"。
- **接管：** 操作员驱动**同一个** live session（不是新会话），完成手动步骤，然后信号恢复。
- **恢复：** 运行从暂停处继续；人类的动作记进证据日志。
- **控制归属：** 系统随时能回答"现在谁在控制、该谁控制"。

操作员控制台故意 mock，但暂停/让出/恢复机制和控制归属模型是真实的。

## 9. 安全与护栏

- **显式、可配置的 allowlist**：允许的域名/路由、允许的动作类型。agent 不能越界——在 agent loop 里强制执行，回放时再查一遍。
- **危险/不可逆动作**（提交、删除、审批）分类并设卡——要么直接拦，要么转人类升级。
- **数据处置 & 静态加密：** 回放必须复现的客户输入值（会员 ID、姓名、金额）写入 artifact 或 ReplayRun 之前，先用 per-tenant 密钥做 AES-256-GCM 加密；回放只在用到的那一刻解密。密钥来自环境变量 / KMS，绝不进 git、也不和 artifact 存一起。提取出的*输出*（余额、账号）带类型、只返回给调用方、绝不持久化。定位策略保持明文（是 UI 结构，不是客户数据），所以 artifact 依然可评审，而敏感载荷即使对内部操作员也保持不透明。

## 10. 异构与多租户（仅设计——不建）

- **界面抽象：** artifact schema 与具体界面无关。`action` 是一小组带类型的动词（click/type/select/read/wait/assert）；`target.strategy` 是唯一漏出具体界面的地方。桌面界面 = 新增一个 `strategy` + 换一个驱动，同一个引擎后面，不是重写。
- **跨租户复用：** 因为我们记的是"意图 + 定位策略"（不是坐标），一个机构实例上录的 artifact 能套到第二个、品牌不同的实例上——路由或标签不同的地方用 per-variant override。这就是"把 /12345 规范成 /:id"的加分项，而且它从 schema 里自然长出来，不用额外做。

## 11. 实施 roadmap

- [x] **Phase 1 — mock 应用。** 建难啃的本地 legacy 应用，埋好错误/结果状态。
- [x] **Phase 2 — agent loop（discovery）。** Playwright + 无障碍树（主）+ K3 视觉（兜底）+ DeepSeek 结构化决策；对 mock 跑通一次真实端到端。
- [x] **Phase 3 — artifact。** Pydantic schema + 把 discovery 运行序列化成 Capability；可评审 + 版本化。
- [x] **Phase 4 — replay。** 确定性 操作→断言→分支 引擎；三态结果契约；错误/恢复处理；每次运行都记录成一条 ReplayRun（结果 + 诊断 + 截图）。
- [x] **Phase 5 — 安全 + 升级 + artifact 管理。** allowlist 强制；暂停/让出/恢复交接状态机（mock 操作员 UI）；一个 CLI 用来 列表 / 改定位 / bump 版本 / dry-run 回放，让目标网站漂移时人工能维持 artifact 最新。
- [x] **Phase 6 — 证据 & 可观测性。** `/evidence/` 放一个 artifact、一份 discovery 日志、一份 replay 日志（含一次命中错误的回放）；成功率统计、失败案例库、以及 回放错误→修复→复查 的闭环。
- [ ] **Phase 7 — REPORT.md。** 把本文档提炼成七个规定标题。

## 12. 决策日志 / 状态

| 日期 | 决策 | 理由 |
|------|------|------|
| 2026-09-17 | 观察 = 无障碍树为主、K3 视觉兜底、截图做证据 | 文本为主在 legacy 上便宜又稳；视觉覆盖无语义界面；截图满足"更丰富信号"要求 |
| 2026-09-17 | 目标 = 本地难啃 mock，不是公开网站 | 完全掌控错误状态以**证明**分类；无 ToS/限流风险 |
| 2026-09-17 | 单进程、六模块；不建基础设施 | 题目明确不建议过早扩展 |
| 2026-09-17 | 栈：Python + Playwright + DeepSeek（决策）+ K3（视觉）+ Pydantic | 成熟、我们熟，K3 补上视觉缺口、免外部 OCR |
| 2026-09-17 | K3 已验证：端点 `api.moonshot.cn`、模型 `kimi-k3`、视觉能读图（"12345"）、推理模型（`reasoning_content` + `content`） | 视觉兜底现在是落地验证过的，不是假设 |
| 2026-09-17 | Phase 2 完成：无障碍树观察（主）+ DeepSeek 结构化决策 + K3 视觉兜底；一次真实端到端（搜 1001 → deactivate）5 步跑通 | loop 是真的，不是草图 |
| 2026-09-17 | discovery 必须容忍 LLM 字段名漂移——DeepSeek 返回了 `text` 而 prompt 要的是 `value` | 印证 artifact 必须**固化**契约；只有确定性回放才能保证一致 |
| 2026-09-17 | K3 视觉兜底在无语义表面返回理解**和**坐标（bbox + 中心 + 归一化） | 回答了「无语义节点怎么定位」——视觉能指到它；artifact 可记图像模板/视觉定位而非 DOM 定位 |
| 2026-09-17 | Phase 3 完成：Pydantic `Capability` schema + `serialize` 提炼 discovery run；终点标记（done/fail）被丢弃、每步断言 + 定位 reasoning 记录 | 天生类型化 / 版本化 / 可评审 |
| 2026-09-17 | legacy 控件 accessible name 可能为空（mock 输入框 `name=""`）——artifact 记 role+name 但回放需要 fallback 链（role 序号 / 相邻标签） | 作为 Phase 4 的具体挑战浮现 |
| 2026-09-18 | artifact 明确可人工管理——设计原则 #5（评审 / 编辑 / 版本）+ Phase 5 artifact 管理 CLI | 「网站变了且我确切知道变在哪」必须是便宜的手改 + 回放，绝不重跑 discovery |
| 2026-09-18 | 可观测性 + 修复闭环（§7）：每次回放记录 ReplayRun；成功率统计标记漂移；失败案例库回放错误；监控→修复→复查 在人工监察下闭环 | 确定性执行器变得可运营：漂移被抓住、诊断、修复、并靠证据复查 |
| 2026-09-18 | 客户数据静态加密（设计原则 #6 + §9）：step 输入值写入前用 per-tenant 密钥 AES-256-GCM 加密；定位策略保持明文；回放仅在用时解密 | 客户 PII 即使对内部操作员也不透明，而 artifact 仍可评审/可修复 |
| 2026-09-18 | Phase 4 完成：确定性回放（操作→断言→分支，无 LLM）+ 三态契约，对 mock 三个埋点验证 success / business_outcome / failure；空 name 的 ordinal fallback + 带边界重试 | 无 LLM 的生产路径是真的，错误分类是被证明的、不是嘴上说的 |
| 2026-09-18 | Phase 5 完成：allowlist 在 loop + replay 强制；AES-256-GCM 静态加密（用时解密）；硬失败时 pause/cede/resume 交接；artifact CLI（list/edit/bump/verify） | 安全、升级、人可管理修复都是真实代码，不是设计笔记 |
| 2026-09-19 | Phase 6 完成：ReplayStore（append-only ReplayRun 记录、inputs AES-256-GCM 静态加密）+ 成功率统计 + 失败案例库 + 回放错误；`/evidence/` 放 artifact、discovery 日志、三份 replay（success / business-outcome / failure）；CLI `telemetry`/`failures`/`replay-case`/`resolve`；修复闭环（漂移 → 失败 → edit+bump → 复查）现场演示 | 每次回放都被记录、度量、可复现——§7 的可观测性 + 修复闭环现在是有真实证据支撑的真实代码，不是设计笔记 |

---

*"砍深度，不要砍掉整块能力。宁可每项核心要求都是薄但真实的版本。"*
