# Python
__pycache__/
*.pyc
.venv/
venv/

# Playwright / browser artifacts
.playwright/
test-results/

# Env / secrets (never commit API keys)
.env
.env.*

# Runtime artifact store (the CLI's working directory)
artifacts/

# Evidence is deliberately committed (it's a deliverable) — do not ignore.

# NOTE: Chinese mirror docs (*.zh.md) are temporarily committed for the
# author's convenience (remote reading on the go). Re-add "*.zh.md" here and
# remove them from the repo before final submission.

# IDE
.idea/

# dashboard runtime screenshots (regenerated by run/replay)
dashboard/screenshots/run_*.png
dashboard/screenshots/*_failure.png
