# Python
__pycache__/
*.pyc
.venv/
venv/

# Playwright / browser artifacts
.playwright/
test-results/

# Env / secrets (never commit API keys)
.env
.env.*
!.env.example

# Interview assignment PDFs and personal docs — do not commit
*.pdf

# The recorded solution paths (artifacts/) are version-controlled — a deliverable.
# Evidence is deliberately committed (raw discovery transcripts + run logs) — do not ignore.

# IDE
.idea/

# dashboard runtime screenshots (regenerated by run/replay)
dashboard/screenshots/run_*.png
dashboard/screenshots/*_failure.png
